CREATE TABLE profile (
  id TEXT NOT NULL PRIMARY KEY,
  name TEXT NOT NULL,
  picture TEXT NOT NULL,
  gender TEXT,
  phone INTEGER,
  branch TEXT
);

select_by_id:
SELECT *
FROM profile
WHERE id = ?;